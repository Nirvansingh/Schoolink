/*
  AI Configuration for SchoolQuest.
  - Keep API keys out of public code in production.
  - When Firebase is added, move this configuration to a secure backend or environment variables.
  - This file is a local placeholder for development only.
*/
window.AI_CONFIG = {
  geminiApiKey: '<YOUR_GEMINI_API_KEY>',
  endpoint: '/api/ai',
  useBackend: false,
  defaultModel: 'gemini-pro',
};
